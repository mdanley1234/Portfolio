---
title: "FTC Command System"
start: "Fall 2024"
end: "Spring 2025"
summary: "A custom command-based control system in Java (30+ files, 2,800+ lines) using a trajectory-driven scheduler for modular robot subsystem control; earned 1st Place Programming at the FTC State Competition."
coverImage: "/images/ftc-command/cover.webp"
layout: "feature"
heroImage: "/images/ftc-command/hero.webp"
heroAlt: "CAD render of the competition robot's chassis — CNC'd aluminum sideplates, the surgical-tubing intake, and the mecanum drivetrain."
tagline: "A command-based control architecture for an FTC robot, built around a trajectory scheduler."
lede: >
  Subsystems that move independently will eventually try to occupy the same
  space, or draw current at the same moment. This architecture makes that
  impossible by construction: every subsystem declares a finite set of
  substates, and a scheduler sequences whole-robot states along a trajectory
  rather than letting a driver command subsystems directly.
facts:
  - label: "Language"
    value: "Java"
  - label: "Size"
    value: "30+ files, ~2,800 lines"
  - label: "Subsystems"
    value: "Generic interface, substates declared as an enum"
  - label: "Coordination"
    value: "BotState objects sequenced by SystemTrajectory"
  - label: "Guarantee"
    value: "Unsafe substate permutations are unreachable"
  - label: "Result"
    value: "1st Place Programming (Control), FTC State Competition"
tags: [Java, Github]
rank: 6
---

## Robot Subsystems

Each subsystem implements a generic `Subsystem` interface and defines an enum of substates representing its finite behaviors. Subsystems are composed into higher-level BotState objects for coordinated behaviors.

<img
    src="/images/ftc-command/graph.webp"
    alt="Graph"
    className="rounded-xl"
/>


## BotState & SystemTrajectory

* **BotState:** encapsulates a set of subsystem substates and transition conditionals (timers, sensor values).
* **SystemTrajectory:** sequences BotState instances to approximate an optimized path between system states — avoids unsafe substate permutations that could cause collisions or brownouts.

## Scheduler & Implementation

* Over 30 Java files and ~2,800 lines of code implementing:
  * trajectory construction,
  * conditional transitions,
  * BotSystem management of active trajectories and user gamepad inputs,
  * subsystem controllers and state transitions.
* The scheduler was tuned for execution headroom and minimal delay while providing reliable transitions.

<img
    src="/images/ftc-command/state.webp"
    alt="Stage"
    className="rounded-xl" 
/>

<img
    src="/images/ftc-command/switch.webp"
    alt="Switch"
    className="rounded-xl"
/>

<img
    src="/images/ftc-command/trajectory.webp"
    alt="Trajectory"
    className="rounded-xl"
/>


## Results

The architecture simplified testing and optimization of individual subsystems and allowed easy integration of advanced sensors. This system contributed to winning the 1st Place Programming (Control) award at the FTC State Competition.

